<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="400dp"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/viewpager_imageview"
        android:layout_width="match_parent"
        android:layout_height="380dp"
        android:layout_marginTop="16dp"
        android:scaleType="centerCrop" />

    <ImageView
        android:id="@+id/x_imageview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/viewpager_imageview"
        android:layout_alignRight="@id/viewpager_imageview"
        android:background="#E14E4646"
        android:visibility="gone"
        app:srcCompat="@android:drawable/ic_menu_close_clear_cancel" />

    <TextView
        android:id="@+id/count_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@id/viewpager_imageview"
        android:layout_alignBottom="@id/viewpager_imageview"
        android:background="#A8050505"
        android:text="1/1"
        android:textColor="#D5FFFFFF" />
</RelativeLayout>